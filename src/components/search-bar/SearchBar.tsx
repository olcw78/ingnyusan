const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="Search Anything!.."
      className="input input-bordered w-full max-w-xs"
    />
  );
};

export default SearchBar;
